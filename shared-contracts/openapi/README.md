# openapi

Spécifications OpenAPI 3 de chaque endpoint REST exposé via la gateway, publiées au moment de l'implémentation (jamais après). Convention : `/api/v1/{module}/...`, erreurs au format RFC 7807.
