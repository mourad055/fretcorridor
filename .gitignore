# Build outputs
target/
build/
dist/
node_modules/
.angular/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store

# Env / secrets
.env
.env.local

# Claude Code local state
.claude/

# Documents de reprise de session, personnels/non partagés
docs/CONTEXTE_SESSION_UI.md
mobile/app_client/pubspec.lock
custom_files/

# pubspec.lock — fichiers generes par Flutter/Dart, source de conflits
# de merge repetes entre les branches Mobile. Chacun regenere le sien
# via 'flutter pub get', pas besoin de le versionner.
mobile/app_chauffeur_transporteur/pubspec.lock
mobile/app_client/pubspec.lock
