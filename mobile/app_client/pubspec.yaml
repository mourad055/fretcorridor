name: fretcorridor_client
description: FretCorridor — Application mobile Client (marketplace)
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  dio: ^5.4.0
  flutter_secure_storage: ^10.3.1
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.6
  image_picker: ^1.1.2
  intl_phone_field: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
